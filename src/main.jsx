import React from 'react'
import ReactDOM from 'react-dom/client'
import BaroldFormulaire from './barold-formulaire.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BaroldFormulaire />
  </React.StrictMode>,
)